<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ERMAD - Home</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <header class="header">
        <h1 class="logo"><img src ="ERMADheader.png"></h1>
         <div class="search-container">
            <form action="/search" method="GET">
            <input type="text" name="q" placeholder="Search ..." >
            <select name="category">
                <option value="">All Categories</option>
                {{#each categories}}
                <option value="{{this.Categoryname}}">{{this.Categoryname}}</option>
                {{/each}}
            </select>
            <button type="submit" >Search</button>
            </form>
        </div>
        <nav class="user-nav">
            <span class= "displayUser" >
                {{#if username}}Hello, {{username}}{{/if}}
            </span>
            <a href="/login" class="logout-btn">LOGOUT</a>
        </nav>
    </header>
            <div class="Main-img">
                <img src="ERMADhome.png" >
            </div>
        <nav class="quick-links">
            <a href="/Materials" class="btn">View All Materials</a>
            <a href="/Discussions" class="btn">View Discussions</a>
            <a href="/MyMaterials" class="btn">View Status of My Materials</a>
            <a href="/EditUploads" class="btn">Edit My Uploads</a>
        </nav>

    <main class="main-content">

 
            </div>
            <div class ="front-button">
                <nav class="quick-links">
                    <div class="upload-dropdown">
                    <button onclick="toggleUploadMenu()" class="btn">Upload</button>
                    <div id="uploadMenu">
                        <a href="/Upload">Upload Material</a>
                        <a href="/Create">Create Post</a>
                    </div>
                    </div>
                </nav>
                <script>
                function toggleUploadMenu() {
                    var menu = document.getElementById('uploadMenu');
                    menu.style.display = (menu.style.display === 'block') ? 'none' : 'block';
                    // Hide menu when clicking outside
                    document.addEventListener('click', function handler(e) {
                    if (!menu.contains(e.target) && e.target.className !== 'btn') {
                        menu.style.display = 'none';
                        document.removeEventListener('click', handler);
                    }
                    });
                }
                </script>
            </div>
        </section>
    </main>

    <script src="../control/main.js"></script>
</body>
</html>